# Authoritative Research State

**Repository:** `bjoern-janson/representation-revision`  
**State date:** 2026-09-03  
**Purpose:** single present-state index for scientific status, provenance, and open frontiers.

This file is a navigation/status document. Historical preregistrations, implementations, result objects, custody records, and claim ceilings remain authoritative within their own jurisdiction.

## Permanent epistemic boundaries

```text
complete over declared DSL != complete over representation space
candidate evaluation != candidate generation
behavioral equivalence != mechanistic equivalence
execution identity != observation identity != interpretation
residual transition structure != Gamma_t
```

The current conceptual decomposition remains:

```text
U_t -> G_t -> R -> A_t -> Gamma_t -> I_t -> X_{>t}
```

with `G_t`, `A_t`, `Gamma_t`, and `I_t` kept distinct.

## Status table

| Object | Status | Authoritative state / ceiling |
| --- | --- | --- |
| Historical three-generator L2 toy assay | EXECUTED / HISTORICAL | Retained as prior evidence; not retrofitted with the later 77-member certificate. |
| Certified finite Boolean universe | FROZEN | `77` syntax members, `6` semantic classes, `d=2`, `max_depth=2`; certification applies only to the declared DSL. |
| Stronger operational W/C/P/E causal assay | PREREGISTERED / UNFIRED | No operational causal execution has been performed; its stronger causal proposition remains unearned. |
| v1 representational accessibility | EXECUTED / FROZEN | Semantic equivalence does not imply equivalence of outgoing transition geometry under the preregistered `R_v1`; at `tau=3`, `755/759` semantic-equivalent pairs have different reachable sets. |
| v2 resistance robustness | EXECUTED / FROZEN | Separation persists across `R_v1`, `R_unit`, and `R_depth` within the preregistered shared structural-edit family. See provenance clarification and post-hoc audit for exact scope. |
| v3 structural-complexity control | SUPPORTED / FROZEN / HISTORICAL | Within the semantic-equivalent pair population, measured outgoing-profile divergence is not fully determined by the declared exact pairwise `|Delta z|` structural-complexity control. |
| `Gamma_t` | OPEN / NOT FROZEN | Fresh scientific ledger only. No statistic, composability control, permutation family, sequence construction, decision rule, execution, or result is frozen. |
| Candidate-space expansion `U_t -> U_{t+1}` | UNESTABLISHED | No result in this repository demonstrates open-ended representation invention or candidate-space expansion. |

## Frozen certificate

The consume-side verifier is bound to:

- certificate source commit: `df450d91d0b3acdd1c0937bd5d8b20bda98b20b4`
- source tree: `df357dac8553979dc281b149ec21c211e01e6a4b`
- frozen syntax count: `77`
- semantic class count: `6`

Downstream certificate consumption verifies literal fixtures and does not call the production universe enumerator.

## v1 — representational accessibility

Frozen branch head:

`freeze-representational-accessibility-v1@8e989205fda77c2ec8e20151541d2232d1234258`

Recorded execution:

```text
semantic-equivalent unordered pairs                759
full labeled resistance-profile divergence         759 / 759
thresholded reachable-set divergence, tau=3       755 / 759
profile divergence with equal reachable sets         4 / 759
```

Historical claim ceiling:

> Within the certified finite Boolean universe, semantic equivalence does not imply equivalence of outgoing transition geometry under the preregistered structural resistance relation.

The post-hoc methodological audit notes that the binary full-profile separation event is partly identity-anchored by labeled self coordinates; the thresholded reachable-set result is not forced by that same argument.

## v2 — resistance-cost robustness

Frozen branch head:

`freeze-resistance-robustness-v1@2807014156e50b86748750c258ebe400345621c9`

Recorded counts:

```text
full-profile separation
R_v1      759 / 759
R_unit    759 / 759
R_depth   759 / 759

reachability separation at tau=3
R_v1      755 / 759
R_unit    755 / 759
R_depth   759 / 759
```

Historical claim ceiling:

> The observed semantic-equivalent separation is not confined to the original `R_v1` cost convention within the finite three-family assay.

Scope clarification: all three families share the broad canonical-AST recursive structural-edit ontology while varying cost/directionality conventions. This is resistance-cost robustness within that family, not evidence for an intrinsic geometry.

Execution-provenance clarification: run `33764556701` successfully completed the robustness assay command before the containing CI job later failed on an exact floating-point equality regression assertion. See `V2_PROVENANCE_CLARIFICATION.md`.

## v3 — declared structural-complexity control

Historical branch head:

`freeze-structural-complexity-control-v1@9421d147010faf9b47a9494b27d0f94360e523a2`

Frozen scientific source used for custodied execution:

`0eead840a1fc2f5de96d809b5e7b45ee14f7d726`

The declared complexity vector is:

```text
z(G) = (
  AST size,
  depth,
  n_INPUT,
  n_NOT,
  n_AND,
  n_OR,
  n_XOR,
  n_EQ,
  n_NEQ
)
```

The exact pairwise conditioning key is componentwise `|Delta z|` within the semantic-equivalent pair population.

Custodied result summary:

```text
family   N_eligible   N_matched   N_separated   matched strata   nonconstant matched strata
R_depth       759          759           759          26                    10
R_unit        759          759           759          26                    11
R_v1          759          759           759          26                    11
```

The all-zero `|Delta z|` stratum is nonconstant for all three families, directly satisfying the preregistered positive decision rule.

Frozen claim ceiling:

> Within the certified finite Boolean universe and across all three preregistered resistance constructions, semantic-equivalent representations remain separated in outgoing transition geometry under exact matching on the declared nine-component structural-complexity difference vector; therefore the observed separation is not fully explained by those declared controls.

This does not establish intrinsic geometry, semantic causality, temporal organization, prediction, behavioral consequence, candidate-space expansion, or a future-relevant invariant.

## v3 custody

Successful custody run:

- run: `33775722525`
- frozen scientific source: `0eead840a1fc2f5de96d809b5e7b45ee14f7d726`
- observer/workflow SHA: `47e432817f1e7791d70e16cc08263bd28f583151`
- artifact ID: `9901478202`
- raw JSON SHA-256: `2ae420019c41bbce43e8e34246d787b26475dc5f3b86f197e01b516ca0b07f72`
- validation: `32 passed in 0.46s`

A byte-exact copy of the GitHub Actions artifact is durably archived under `evidence/v3/`; see `evidence/v3/CUSTODY_RECORD.md`.

## Post-hoc methodological audit

`METHODOLOGICAL_AUDIT.md` is explicitly **POST-HOC / NON-AUTHORIZING**. It records four scope properties without changing historical science:

1. forced self-coordinate contribution to labeled full-profile separation;
2. v3 conditioning by pairwise `|Delta z|` rather than absolute complexity-pair identity;
3. shared structural-edit ontology across the three v2 families;
4. mixed BIT/BOOL membership in the 77-node profile destination universe.

The audit is a precision layer, not a new result.

## Current open frontier — `Gamma_t`

Current branch ledger:

`preregister-gamma-t-v0@e154cd7d791a23d40463fc513e64e3d7b798d845`

Standing boundary:

```text
residual transition structure != Gamma_t
```

The fresh ledger asks whether ordering of a fixed finite transition inventory contains measurable structure beyond a preregistered order-free control. It intentionally leaves the statistic, composability control, permutation family, sequence construction, and decision rule open.

No `Gamma_t` result exists.

## Repository repair boundary

Repository-cleanup commits made after the scientific objects are custody/governance/presentation surfaces only. They do not become scientific-source commits by virtue of being later in Git history.

The current repair design and implementation plan live under `docs/superpowers/` and explicitly prohibit new scientific design during consolidation.
