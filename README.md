# Representation Revision (L2 Assay)

Minimal isolated assay for the frozen L2 claim:

> Corrective evidence can cause persistent revision of a representation generator.

The repository deliberately contains no L3 machinery. The declared revision universe is finite, deterministic, and fixed independently of the current generator. Scientific execution is separated from pre-certification and recorded as an immutable artifact.
