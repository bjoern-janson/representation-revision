# Representation Revision — Frozen Finite-Universe Ladder

This repository is an evidence-first finite research program about representation revision, accessibility, and the boundary between **what can be selected or traversed inside a frozen representation universe** and **what remains unestablished beyond that universe**.

The authoritative present-state index is [`RESEARCH_STATE.md`](RESEARCH_STATE.md). The machine-readable frozen-object index is [`SCIENTIFIC_CUSTODY_MANIFEST.json`](SCIENTIFIC_CUSTODY_MANIFEST.json). The later main-line integration is recorded separately in [`MAIN_INTEGRATION_RECORD.md`](MAIN_INTEGRATION_RECORD.md).

> **Current scientific state:** v1, v2, v3, and Γ ordering v1 are executed and frozen; Γ has a separately frozen post-hoc algebraic explanation; **no Γ+1 is authorized**.

The unresolved frontier remains:

```text
G* not in U_t
      ?
      ↓
G* in U_(t+1)
```

Nothing in the completed finite-universe program establishes that arrow.

## Permanent boundary calculus

```text
observation
    != mechanistic explanation
    != causal interpretation
    != future research authorization
```

and:

```text
selection                  != newly constructible
reachability change        != newly constructible
ordering sensitivity       != orientation
orientation                != causality
causality                  != adaptation
adaptation                 != U_t -> U_(t+1)
```

These are scientific claim boundaries, not rhetorical caveats.

## Current scientific state

```text
historical three-generator toy assay       EXECUTED / HISTORICAL
certified Boolean universe                  FROZEN
stronger operational W/C/P/E assay          PREREGISTERED / UNFIRED
v1 representational accessibility           EXECUTED / FROZEN
v2 resistance-cost robustness               EXECUTED / FROZEN
v3 structural-complexity control            SUPPORTED / FROZEN
Gamma ordering v1                           SUPPORTED / FROZEN
Gamma post-hoc algebraic explanation        FROZEN / NON-AUTHORITY-EXPANDING
Gamma+1                                     NOT AUTHORIZED
candidate-space expansion U_t -> U_(t+1)    UNESTABLISHED
```

## Scientific objects kept separate

This repository contains three distinct scientific lineages that do not inherit one another's evidential credit:

1. **Historical bounded-revision toy assay** — a prior result over a hand-declared three-generator candidate set. See [`RECORDED_EXECUTION.json`](RECORDED_EXECUTION.json) and [`EXECUTION_PROVENANCE.md`](EXECUTION_PROVENANCE.md).
2. **Operational W/C/P/E causal assay** — preregistered but **unfired**. See [`PREREGISTRATION.md`](PREREGISTRATION.md) and [`OPERATIONAL_PREREGISTRATION.json`](OPERATIONAL_PREREGISTRATION.json).
3. **Certified finite-universe accessibility ladder** — v1 → v2 → v3 → Γ → post-hoc explanation → STOP.

The operational causal assay is not retroactively credited by the accessibility ladder, and the historical toy result is not upgraded into candidate-space generation.

## Certified finite universe

The consume-side certificate freezes:

```text
syntax members       77
semantic classes      6
d                      2
max depth              2
```

[`certificate/verify.py`](certificate/verify.py) validates the literal frozen fixtures and certificate bindings without regenerating the universe.

Permanent guardrail:

> **Complete over the declared DSL does not imply complete over representation space.**

## v1 — semantic equivalence != outgoing transition geometry

Recorded finite result:

```text
semantic-equivalent unordered pairs              759
full outgoing-profile divergence               759 / 759
thresholded reachable-set divergence, tau=3    755 / 759
profile-only divergence                           4 / 759
```

Supported finite statement:

> **Within the certified finite Boolean universe, semantic equivalence does not imply equivalence of outgoing transition geometry under the preregistered structural resistance relation.**

Authority:

- [`ACCESSIBILITY_PREREGISTRATION.json`](ACCESSIBILITY_PREREGISTRATION.json)
- [`RECORDED_ACCESSIBILITY_EXECUTION.json`](RECORDED_ACCESSIBILITY_EXECUTION.json)
- [`ACCESSIBILITY_EXECUTION_PROVENANCE.md`](ACCESSIBILITY_EXECUTION_PROVENANCE.md)
- canonical result SHA-256: `c7cce848ebfcd064c4579085ffd4980ade0e27e9f18e9c1dcf4cbf1120a7ecc8`

Ceiling: finite and relation-relative; no intrinsic geometry, future prediction, behavioral causality, or representation-space completeness.

## v2 — separation survives resistance-convention pressure

Three frozen structural resistance families were tested: `R_v1`, `R_unit`, and `R_depth`.

```text
full-profile separation
R_v1      759 / 759
R_unit    759 / 759
R_depth   759 / 759

reachability separation at tau=3
R_v1      755 / 759
R_unit    755 / 759
R_depth   759 / 759
```

Supported finite statement:

> **Semantic-equivalent separation of outgoing transition geometry is not confined to the original `R_v1` convention within this frozen three-family structural-edit assay.**

The families share a broad canonical-AST structural-edit ontology. This is not evidence for an intrinsic or uniquely correct geometry.

The historical workflow nuance is preserved in [`V2_PROVENANCE_CLARIFICATION.md`](V2_PROVENANCE_CLARIFICATION.md).

## v3 — declared structural-complexity control is insufficient

The preregistered control exactly conditions semantic-equivalent pairs on componentwise pairwise `|Delta z|` for a nine-coordinate AST complexity vector.

```text
family   N_eligible   N_matched   N_separated   matched strata   nonconstant matched strata
R_depth       759          759           759          26                    10
R_unit        759          759           759          26                    11
R_v1          759          759           759          26                    11
```

Supported finite statement:

> **Within the certified finite universe and frozen resistance families, the declared pairwise structural-complexity difference vector is insufficient to determine the measured magnitude of outgoing-profile divergence.**

Frozen scientific source: `0eead840a1fc2f5de96d809b5e7b45ee14f7d726`

Custody:

```text
run                    33775722525
observer/workflow SHA  47e432817f1e7791d70e16cc08263bd28f583151
pytest                 32 passed in 0.46s
raw JSON SHA-256       2ae420019c41bbce43e8e34246d787b26475dc5f3b86f197e01b516ca0b07f72
```

The exact v3 evidence is durably archived under [`evidence/v3/`](evidence/v3/).

Ceiling: no intrinsic geometry, semantic causality, temporal organization, prediction, behavior, or candidate-space expansion.

## Γ ordering v1 — fixed transition multiset != fixed ordered functional

For each eligible motif with center `c` and three distinct semantically equivalent leaves, the exact null ensemble contains all six `S3` permutations:

```text
(c, pi_1, c, pi_2, c, pi_3, c)
```

Every permutation preserves the same directed transition multiset, identities, visit counts, start/end state, and frozen resistance objects.

The preregistered statistic was:

```text
L_k(i,j) = sum_r |R_k(G_i,G_r) - R_k(G_j,G_r)|
Q_k      = sum_(t=0)^4 L_k(G_t,G_(t+2))
```

Primary rule:

```text
common_positive_motifs > 0
```

Frozen result:

```text
eligible motifs                  116,664
common-positive motifs           116,475
primary criterion                SATISFIED

R_v1 order-sensitive motifs      116,475
R_unit                           116,475
R_depth                          116,592
```

Supported finite statement:

> **Within the certified finite Boolean universe, there exists a semantically invariant, exactly transition-matched composable thread for which the preregistered second-order outgoing-profile displacement depends on transition ordering across all three frozen resistance conventions.**

Authority:

- [`GAMMA_PREREGISTRATION.md`](GAMMA_PREREGISTRATION.md)
- [`GAMMA_ORDERING_PREREGISTRATION.json`](GAMMA_ORDERING_PREREGISTRATION.json)
- scientific source: `389969e510446450684402be3c1df4ececcf0ed7`
- custody run: `33844036424`
- [`GAMMA_ORDERING_RESULT.json`](GAMMA_ORDERING_RESULT.json)
- [`GAMMA_ORDERING_ADJUDICATION.md`](GAMMA_ORDERING_ADJUDICATION.md)
- raw result SHA-256: `abc55c8d63f055e9681b6feeef926f8bbe9e026368d974f3f0fbb18b70ca01f5`

Permanent ceiling:

```text
ordering / adjacency sensitivity
    != orientation
    != arrow of time
    != causality
    != adaptation
    != candidate-space expansion
```

## Γ post-hoc algebraic explanation — non-authority-expanding

After the primary result and adjudication were frozen, Γ was reduced exactly.

For:

```text
Gamma = (c,a,c,b,c,d,c)
```

because `L_k(c,c)=0`:

```text
Q_k(a,b,d) = L_k(a,b) + L_k(b,d)
```

Across `S3`:

```text
Q_k nonconstant
    iff
L_k(a,b), L_k(a,d), L_k(b,d) are not all equal.
```

Thus the positive Γ result is fully explained by non-equilateral static `L_k` geometry plus the permutation/order operation. No additional temporal state, orientation, or latent dynamic mechanism is required.

Authority:

- [`GAMMA_POSTHOC_ALGEBRAIC_CHARACTERIZATION.md`](GAMMA_POSTHOC_ALGEBRAIC_CHARACTERIZATION.md)
- explanation commit: `18433568be5c8e211060f326140f8c6b4650e648`
- frozen scientific closure: `ad46551e8ce4758e8608738007380a604e77eaf9`

The explanation tightens interpretation; it does not replace the preregistered result or expand its authority.

## Post-hoc methodological audit

[`METHODOLOGICAL_AUDIT.md`](METHODOLOGICAL_AUDIT.md) remains **POST-HOC / NON-AUTHORIZING**. It sharpens the scope of v1-v3 without rerunning or re-adjudicating those results.

## STOP

```text
v1  semantic equivalence != outgoing transition geometry
 ↓
v2  separation survives three frozen resistance conventions
 ↓
v3  declared structural-complexity control is insufficient
 ↓
Γ   fixed transition multiset does not fix the ordered Q functional
 ↓
post-hoc algebraic reduction explains Γ from static L_k heterogeneity + ordering
 ↓
STOP
```

No `Γ+1`, directionality statistic, predictive interpretation, or successor temporal object is authorized by the completed lineage.

Any future experiment requires a **new scientific question, new preregistration, and new authorization**.

## Open frontier

```text
G* not in U_t
      ?
      ↓
G* in U_(t+1)
```

Known non-sufficient substitutes include:

```text
selection among pre-existing candidates
activation of a dormant constructor
reachability change inside frozen U
recombination inside a supplied DSL
hidden answer with exposed hypothesis family
ordering-sensitive composition of static relations
```

The `?` is authoritative.

## Custody and integration

Scientific authority remains content-addressed:

```text
observation != explanation != causal interpretation != future authorization
```

The original Γ lineage remains intact in history:

```text
05f718cba12194a42c1578f790081cc9016a151f  preregistration freeze
389969e510446450684402be3c1df4ececcf0ed7  scientific source
0a648fad0ad3b23bfd4f9ecc4e8e3acaff4598ce  result/adjudication
18433568be5c8e211060f326140f8c6b4650e648  post-hoc explanation
ad46551e8ce4758e8608738007380a604e77eaf9  scientific closure
```

[`SCIENTIFIC_CUSTODY_MANIFEST.json`](SCIENTIFIC_CUSTODY_MANIFEST.json) is preserved byte-for-byte as the custody index frozen before main-line integration. Its historical `main_merge_authorized: false` field records that earlier state and is not retroactively rewritten. The later authorization and integration parents are recorded in [`MAIN_INTEGRATION_RECORD.md`](MAIN_INTEGRATION_RECORD.md).

The `archive/*` refs are navigation aids only; exact commit SHAs and recorded artifact hashes remain authoritative.

The historical Γ custody workflow is imported unchanged. It is branch-scoped to `freeze-gamma-ordering-v1`, so ordinary changes on `main` do not execute Γ.

## Repository map

| Path | Role |
| --- | --- |
| [`RESEARCH_STATE.md`](RESEARCH_STATE.md) | Authoritative present-state index |
| [`SCIENTIFIC_CUSTODY_MANIFEST.json`](SCIENTIFIC_CUSTODY_MANIFEST.json) | Frozen pre-integration scientific-object/hash index |
| [`MAIN_INTEGRATION_RECORD.md`](MAIN_INTEGRATION_RECORD.md) | Non-scientific record of main-line integration |
| [`certificate/`](certificate/) | Frozen finite Boolean universe and verifier |
| [`evidence/v3/`](evidence/v3/) | Durable v3 custody archive |
| [`V2_PROVENANCE_CLARIFICATION.md`](V2_PROVENANCE_CLARIFICATION.md) | Historical workflow provenance clarification |
| [`METHODOLOGICAL_AUDIT.md`](METHODOLOGICAL_AUDIT.md) | Post-hoc, non-authorizing v1-v3 scope audit |
| [`GAMMA_ORDERING_PREREGISTRATION.json`](GAMMA_ORDERING_PREREGISTRATION.json) | Γ machine-readable frozen protocol |
| [`GAMMA_ORDERING_RESULT.json`](GAMMA_ORDERING_RESULT.json) | Γ frozen observation |
| [`GAMMA_ORDERING_ADJUDICATION.md`](GAMMA_ORDERING_ADJUDICATION.md) | Γ primary adjudication/ceiling |
| [`GAMMA_POSTHOC_ALGEBRAIC_CHARACTERIZATION.md`](GAMMA_POSTHOC_ALGEBRAIC_CHARACTERIZATION.md) | Γ later explanatory reduction |
| [`GAMMA_SCIENTIFIC_LEDGER.md`](GAMMA_SCIENTIFIC_LEDGER.md) | Γ authority chain and closure ledger |
| [`tests/`](tests/) | Regression and contract tests |
| [`.github/workflows/`](.github/workflows/) | Ordinary CI plus explicit historical/custody workflows |

## Verification

Ordinary regression:

```bash
python -m pytest
```

Historical scientific workflows remain separate from generic CI. Γ is already frozen and custodied and must not be re-executed merely because its lineage is now reachable from `main`.
