# Valid L2 Execution Provenance

The earlier pre-v2 rehearsal was discarded because its future set reused diagnostic events. It is not part of the scientific record.

## Valid historical sequence

1. Corrected preregistration v2 was committed before valid execution at Git commit `2190b916e37b39198689b4b2e9f793f6a0991b7e`.
2. Pre-certification was then executed and passed for the finite universe, deterministic enumeration, coverage independence, unique discrimination, and adoption.
3. The recorded pre-certification artifact was committed at `ac90aad8c03e18519c86639873999fc74c4598b4`.
4. The valid scientific execution was run only after that checkpoint and committed at `03e837ec7c5a3c6e5feeb199e2c88af283e90e81`.

## Historical scientific result

- diagnosis: `generator-failure`
- original generator: `g0_surface`
- adopted generator: `g1_complete`
- coverage: `certified-complete` for the declared finite toy universe
- unique discriminator: `g1_complete`
- diagnostic mode: `0`
- future mode: `1`
- future adaptive and shadow inputs are exactly paired and disjoint from the diagnostic event set
- all four held-out future events produced representations different from the fixed-`g0` shadow

This historical record makes no L3 claim. The candidate universe was fixed before outcome observation and was not expanded after the result.

## Consolidated repository provenance

The two research branches were preserved in a two-parent merge at:

`df450d91d0b3acdd1c0937bd5d8b20bda98b20b4`

with parents:

- `3973a847e5d5cf05e761b60abdd7408895e2cdb7` — `l2-assay`
- `cce8bd5ed46e07a7a0faf0496c91e7c783711e8e` — `l2-boolean-assay`

A superseded intermediate merge commit was abandoned and is not the authoritative consolidated state.

## Frozen universe provenance

The Boolean universe certificate records `df450d91d0b3acdd1c0937bd5d8b20bda98b20b4` as its scientific source baseline. The certificate-containing repository state was subsequently extended with the frozen fixtures and consume-only verifier; these later commits do not rewrite or re-run the historical causal execution.

The operational preregistration is frozen separately in [`OPERATIONAL_PREREGISTRATION.json`](OPERATIONAL_PREREGISTRATION.json) and [`PREREGISTRATION.md`](PREREGISTRATION.md). It defines the stricter W/C/P/E burden, temporal anti-leakage rule, shadow control, compensation control, and hard stop conditions for the next operational assay.

## Scientific boundary

The project deliberately keeps the following propositions distinct:

```text
ΔB does not prove ΔG.
ΔG does not by itself prove Cause(e, ΔG).
Cause(e, ΔG) does not by itself establish persistence.
Persistence does not by itself establish a G1-specific held-out consequence.
```

The frozen Boolean certificate establishes only bounded completeness for its declared DSL. It does not establish completeness over all possible representations.
